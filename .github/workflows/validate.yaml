name: Test and validate on push

on:
  workflow_dispatch:
  push:
    branches:
      - '**'
    tags-ignore:
      - '**'

jobs:
  test:
    uses: ./.github/workflows/test.yaml
    with:
      poetry-version: "1.8.2"
