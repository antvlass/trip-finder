# Required
URL_API=https://your-flight-api-url

# Django
ALLOWED_HOSTS=localhost,127.0.0.1

# Airport autocomplete (optional)
# When running in Docker with --network appnet, use the container name as host
# AIRPORTS_DB_HOST=postgres
# AIRPORTS_DB_PORT=5432
# AIRPORTS_DB_NAME=postgres
# AIRPORTS_DB_USER=postgres
# AIRPORTS_DB_PASSWORD=
